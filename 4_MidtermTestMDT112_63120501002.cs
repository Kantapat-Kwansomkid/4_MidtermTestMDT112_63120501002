using System;

namespace _4_MidtermTestMDT112_63120501002
{
    class Program
    {
        static void Main(string[] args)
        {
            Define floats = x, y, dx, dy, step;
            Input floats = x1, y, x2, y2;
            dx = x2 - x1;
            dy = y2 - y1;
            is abs(dx) >= abs(dy);
            step = abs(dx);
            step = abs(dynamic);
            dx /= step;
            dy /= step;
            x = x1;
            y = y1;
            Define number i = 1
            Is i <= step
            
            x += dx
            y += dy


            if (is abs(dx) >= abs(dy))
            {
                Console.WriteLine("step = abs(dy)");
            }
            else
            {
                Console.WriteLine("step = abs(dx)");
            }
            Console.ReadLine();

        }
    }
}
